# Resend API key for sending invoice emails
# Get your key at https://resend.com/api-keys
# For production: use `supabase secrets set RESEND_API_KEY=your_key`
RESEND_API_KEY=

# Optional: frontend origin for CORS (e.g. https://app.example.com). If unset, Allow-Origin is *.
# For production: use `supabase secrets set FRONTEND_URL=https://your-app-url`
# FRONTEND_URL=

# Optional: secret for scheduled period-invoice generation. If set, POST to generate-period-invoices must include header x-cron-secret.
# For production: use `supabase secrets set CRON_SECRET=your_secret`
# CRON_SECRET=
